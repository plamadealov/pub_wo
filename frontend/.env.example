# Опционально: абсолютный URL API (без завершающего /). Тогда запросы идут напрямую на бэкенд;
# на сервере нужен CORS (ALLOW_ORIGINS с URL фронта). Пусто — относительный /api (Vite proxy / Netlify rewrite).
# VITE_API_BASE_URL=https://your-api.example.com
